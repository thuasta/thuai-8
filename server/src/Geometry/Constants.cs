namespace Thuai.Server.Geometry;

public static class Constants
{
    // The error margin for floating point calculations.
    public const decimal CalculationErrorMargin = 1e-6m;

    // The error margin for the results compared to real result.
    public const decimal ResultErrorMargin = 1e-3m;
}
